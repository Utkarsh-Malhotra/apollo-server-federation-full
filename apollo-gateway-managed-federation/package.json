{
  "name": "apollo-gateway",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compose-graph": "rover supergraph compose --config ./supergraph-config.yaml > supergraph.graphql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "^0.48.1",
    "@apollo/subgraph": "^0.3.1",
    "apollo-server": "^3.6.3",
    "dotenv": "^16.0.0"
  }
}
